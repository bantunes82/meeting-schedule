services:
  postgres:
    image: 'postgres:18.1'
    environment:
      - 'POSTGRES_DB=meeting_schedule'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
